// /dashboard/profile only redirects, to Voice's cast (RETIRED_ADDRESSES in
// lib/nav.ts), so while it resolves it shows Voice's skeleton rather than the
// Overview one it would otherwise inherit.
export { default } from '../voice/loading'
