import { SurfaceShell } from '@/components/shell/surface-shell'

// This week — "what needs attention this week?" A new address in Phase 1.
// WP15 fills it; the anomaly flags WP8 writes are its first section.

export default function Page() {
  return <SurfaceShell nav="week" />
}
